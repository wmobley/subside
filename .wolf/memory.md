# Memory

> Chronological action log. Hooks and AI append to this file automatically.
> Old sessions are consolidated by the daemon weekly.

| 11:16 | Anonymous-user pixel-value interpretation popup: click a Displacement/Velocity pixel or select an address-search result that lands on one → popup with the real sampled value + plain-language subsidence interpretation (gated to `!isAuthed`). Confirmed via gdalinfo that OPERA DISP COGs are UTM (EPSG:326xx/327xx), not 4326 — pixelSample.js reprojects via proj4 using the zone derived from the EPSG code. Verified sampling math against 4 known pixels via local GDAL ground truth (exact match) using a Range-request-capable local server (python http.server does NOT support Range and silently corrupts geotiff.js reads — use `http-server`/nginx/real hosting for any future COG-sampling test). | ui/src/lib/pixelSample.js (new), ui/src/lib/layerContext.js (new, extracted from SubsideAnalysis.jsx), ui/src/components/mapworkbench/StacCogLayer.jsx, ui/src/components/mapworkbench/StacResults.jsx, ui/src/components/mapworkbench/AddressSearch.jsx, ui/src/components/mapworkbench/ModelMap.jsx, ui/package.json (+proj4 direct dep) | lint+build pass, sampling math verified against GDAL ground truth | ~180k |
| 12:15 | Bug fix (bug-001): user saw the full run-metadata popup (acquisition window, location, OPERA products, frame, run ID, layer range, bbox, button) plus the new interpretation block for anonymous users — wanted ONLY value + rate explanation. Replaced the append-on-top approach with an early `!isAuthed` branch in RunDetailsPopup rendering a separate minimal PixelValuePopup; authed view unchanged. Removed now-unused `.stac-run-popup-interpret` CSS. | ui/src/components/mapworkbench/StacResults.jsx, ui/src/styles.css | lint+build pass | ~15k |

## Session: 2026-07-27 12:34

| Time | Action | File(s) | Outcome | ~Tokens |
|------|--------|---------|---------|--------|
| 12:51 | Bug fix (bug-002): user clicked a visibly-rendered Displacement pixel and got "value unavailable" — root cause was most displacement runs render via the cheap PNG ImageOverlay path, which never sampled anything. Added `sampleCogValueAt(href, lat, lon)` to pixelSample.js (lazily parses just the run's underlying COG, cached per href) and wired it into the ImageOverlay click handler (upgrades the open popup once resolved) and the address-search probe effect (checks overlay-backed runs by bbox after registered COG samplers). Caught and fixed a bundle-size regression along the way: a static `import ... from 'georaster'` pulled the ~580KB georaster bundle into the main chunk — switched to the same dynamic `import('georaster')` StacCogLayer.jsx already uses. | ui/src/lib/pixelSample.js, ui/src/components/mapworkbench/StacResults.jsx | lint+build pass (chunk sizes verified back to baseline), sampleCogValueAt verified against GDAL ground truth incl. cache hit | ~90k |
| 14:07 | Decision (see cerebrum.md Decision Log): user asked "why wouldn't we use the COG since it's Cloud Optimized" after the bug-002 explanation, then explicitly said "Render the Cogs" — overriding the perf/percentile-clip tradeoff I'd raised. renderRun now always uses StacCogLayer for displacement when a COG asset exists (matching velocity); PNG overlay is fallback-only for runs with no COG at all. Removed the now-dead `sampleCogValueAt`/lazy-overlay-sampling code from pixelSample.js and StacResults.jsx (bug-002's fix), since every COG-backed run is now registered via the existing onReady/samplersRef path from the start. | ui/src/components/mapworkbench/StacResults.jsx, ui/src/lib/pixelSample.js | lint+build pass, main bundle size unchanged from baseline | ~35k |
| 14:14 | UI fix: per-run label in the Layers panel (`runRowLabel`) was one long string ("2025-06-01 → 2025-09-01 · Kerr County, Texas · Frames 1096, 30452 · run dffd251c-007") truncated by `.slp-row .slp-name`'s `nowrap`/ellipsis — user reported it as "unreadable." Split into `runRowParts()` returning {title, dates, meta}: bold location as title line, "Dates: ..." line, "Frames ... · run ..." line; falls back to the date range as title when no location resolved (skipping the redundant dates line then). Gotcha caught before shipping: `.slp-run-name` alone (0,1,0 specificity) would've been silently overridden by the existing `.slp-row .slp-name` rule (0,2,0) — used `.slp-run-row .slp-name` instead to match/beat that specificity. | ui/src/components/mapworkbench/StacResults.jsx, ui/src/styles.css | lint+build pass; logic hand-traced against the user's exact example (Kerr County) | ~25k |

## Session: 2026-07-27 14:16

| Time | Action | File(s) | Outcome | ~Tokens |
|------|--------|---------|---------|--------|
| 14:35 | Bug fix (bug-003, critical): user "Clicking doesn't work to show up a popup at all" after the COG-everywhere change. Root cause: `georaster-layer-for-leaflet@4.1.2` (checked both actual dist entry points, not just one bundle) has ZERO click/interactive-target support — `layer.on('click', ...)` in StacCogLayer.jsx was a silent no-op from day one, masked because displacement mostly rendered via the (working) ImageOverlay path until "Render the Cogs" made it universal. Fixed by moving interactivity to the map level: `useMapEvents({click, contextmenu})` in StacResults.jsx + new shared `findSampledRunAt(lat,lon)` helper reusing the existing samplersRef registry (same bounds+sampleAt mechanism built for the address-search probe). Removed the dead onClick/onContextMenu props+wiring from StacCogLayer.jsx entirely. Verified with a REAL Playwright/Chromium click (not a synthetic call) against a temporary harness mounting the actual component + a local range-request-capable COG server — click reached useMapEvents, bounds.contains() matched, sampleAt() returned the exact GDAL ground-truth value. Harness + playwright (--no-save) fully removed after. | ui/src/components/mapworkbench/StacCogLayer.jsx, ui/src/components/mapworkbench/StacResults.jsx | lint+build pass; REAL browser click verified end-to-end | ~110k |
| 14:40 | Investigated user's nodata-display question + resampling question (no code change for the first — explained current transparent-nodata behavior and offered options). For "is COG zoom nearest-neighbor or other": traced actual code — GDAL COG overview pyramid uses CUBIC (confirmed via gdalinfo metadata OVERVIEW_RESAMPLING on the real local COG), but georaster-layer-for-leaflet's client-side tile resampling defaults to nearest ("near-vectorize", from georaster-stack/core/index.js:45) since StacCogLayer.jsx never set `resampleMethod`. | (none — investigation only) | n/a | ~20k |
| 14:47 | Implemented per user request: `resampleMethod: 'bilinear'` in StacCogLayer.jsx's GeoRasterLayer options. Verified safety first (NaN-blending risk) with a standalone geowarp test using real distinct values either side of a NaN gap — confirmed geowarp excludes NaN from the interpolation weighting (no wrong/garbage finite colors at data-gap edges), so bilinear is safe here since this raster's nodata sentinel is literal IEEE NaN (confirmed via gdalinfo). Separately, user asked whether images are color-normalized across a shared max range — found and fixed a real bug: `displacementLegend`/`velocityLegend` (combinedRange across all currently-shown runs) were only used for the panel's legend labels, while each StacCogLayer was actually colored using its OWN per-run range — meaning the legend didn't match what was rendered, and different runs weren't color-comparable. Now both branches pass the shared combined range (falling back to per-run range if none available). | ui/src/components/mapworkbench/StacCogLayer.jsx, ui/src/components/mapworkbench/StacResults.jsx | lint+build pass; NaN-safety verified via standalone geowarp test | ~45k |
| 16:18 | Bug fix (bug-005, production): user hit a live CORS error loading `texas_counties` MVT tiles, guessed CKAN rate-limiting — it wasn't. Traced to the FastAPI app's CORSMiddleware (SUBSIDE_CORS_ORIGINS, already correctly deployment-derived). User then pasted the live pod's actual CORS config, revealing a SECOND, separate CORS layer: Tapis Pods' own ingress-level `cors_allow_origins`, stuck at `["https://subside.pods.portals.tapis.io"]` (stale, pre-dating the subsideapi/subsideui pod split) — register_pods.py never set this field at all. Added `"cors_allow_origins": [ui_url]` to the api pod spec in build_specs(), verified via `--dry-run` it now computes correctly to `["https://subsideui.pods.portals.tapis.io"]`. NOT applied to the live pod yet — needs `python tapis/register_pods.py --pods api --restart` with Tapis credentials + the user's explicit go-ahead (production deployment action). | tapis/register_pods.py | py_compile + --dry-run pass; live deploy pending user approval | ~30k |
| 16:30 | Bug fix (bug-006, regression from bug-005): user ran the update — live subsideapi's cors_allow_methods/headers came back EMPTY. Tapis Pods' update_pod() appears to replace the whole cors_allow_* block rather than merge per-field, so passing only cors_allow_origins wiped methods/headers to []. Gave user exact restore JSON (reconstructed from what they'd originally pasted) for immediate manual use, then fixed build_specs() to set cors_allow_methods/headers/credentials explicitly alongside origins so a future update can't repeat this. Verified via --dry-run. Also checked stac-platform/deploy/register_pods.py — same gap exists there (never sets these fields either) but user hasn't confirmed stacapi's actual live values, so gave that as an unverified guess only, not applied. | tapis/register_pods.py | py_compile + --dry-run pass; live deploy pending user approval | ~35k |
| 16:44 | Bug fix (bug-007): after CORS fixed, user reported map still showing missing/blank raster blocks, attached a HAR guessing CKAN rate-limiting. Analyzed the HAR directly: 32 distinct COG files loading at once, all HTTP/1.1 to ckan.tacc.utexas.edu (~6-connection-per-origin browser cap), requests taking 200ms-1.5s each, bursts of many simultaneous requests per file, zero actual HTTP errors recorded (connection-limit starvation, not failures). User's framing was the key insight: COGs stream fine individually (their own 22GB example) — the bug is firing ~24-32 simultaneous opens with no coordination. Added ui/src/lib/loadQueue.js (withLoadSlot, MAX_CONCURRENT_LOADS=4) — every StacCogLayer's open+header-read now goes through this shared gate, staggering into waves of 4; verified via a standalone Node test (10 tasks, limit 4: peak concurrency never exceeded 4, a pre-cancelled task correctly never ran). Also widened tileerror retry backoff from fixed-1s×3 to 1s/2s/3s/4s/5s×5. | ui/src/lib/loadQueue.js (new), ui/src/components/mapworkbench/StacCogLayer.jsx | lint+build pass; concurrency-limit logic verified via standalone test | ~55k |
| 17:16 | Investigation (bug-008, unresolved): user proactively asked "on zoom are we reloading that stack of rasters?" Built a real Playwright test (20 StacCogLayer instances against a local range-capable server, then a programmatic zoom) — measured peak concurrency 37 during zoom vs 11 during the already-throttled initial load, confirming bug-007's fix doesn't cover this. Attempted a fix: ui/src/lib/fetchGate.js patching window.fetch globally (gating any Range-header request), removing the narrower per-layer queue since the fetch-level gate should have superseded it. Verified via the SAME test harness this had ZERO effect (peak concurrency got WORSE, 59) despite debug logs confirming the patch correctly intercepted some requests. Root-caused via node_modules source: georaster-stack spawns 3 Web Workers PER LAYER and reads tile data from inside them — invisible to any main-thread fetch/XHR patch. Reverted fetchGate.js entirely, restored the verified-working per-layer withLoadSlot wrapping from bug-007 with a comment documenting the worker-pool limitation. Test-harness bugs found+fixed along the way: (1) differentiating test COG URLs with `?instance=N` broke georaster's naive `url+'.ovr'` string-concat overview probe; (2) `window.__readyCount = 0` at the top of a React render body silently resets on every re-render, masking real progress as "stuck at 0". Saved a cross-session project memory (subside-cog-worker-concurrency.md) since this is a real, unresolved, hard-to-rediscover limitation. | ui/src/components/mapworkbench/StacCogLayer.jsx (reverted to bug-007 state), ui/src/lib/loadQueue.js (comment/value revert) | lint+build pass; zoom-burst risk CONFIRMED but NOT fixed — needs user direction (Service Worker vs fewer live COGs) | ~90k |
| 17:31 | Feature (addresses bug-008 architecturally, not just via network throttling): user chose to reduce concurrent live COGs via a UX flow rather than a Service Worker. Confirmed 3 design decisions via AskUserQuestion: (1) per-run visibility becomes opt-in — only the single most-recently-acquired run per kind (displacement/velocity) renders by default, rest are manual-check; (2) added a date-range preset selector (All time/3mo/6mo/1yr) that narrows the actual STAC search (bbox+datetime), not just the rendered list; (3) added a location text filter (client-side, over itemMeta().location) since STAC search itself is bbox- not location-based. Replaced the old opt-out `hiddenIds` state with opt-in `shownIds`+`pickedKinds` (isRunVisible/toggleRun) — first manual toggle for a kind seeds shownIds with the previous auto-default so flipping a different run on doesn't silently hide the one already showing. Verified the new state-machine logic (default-visible, seed-on-first-toggle, unseeded-toggle-off, per-kind independence) via a standalone Node simulation of the exact logic before trusting it. | ui/src/components/mapworkbench/StacResults.jsx, ui/src/styles.css | lint+build pass; visibility state-machine verified via standalone simulation across 4 scenarios | ~60k |
