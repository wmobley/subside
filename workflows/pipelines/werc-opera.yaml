---
# Pipeline B: SUBSIDE WERC OPERA — full stack / reference / velocity / export.
#
# Flow: discover → download-opera → build-stack → compute-reference → estimate-velocity → export-geotiffs → publish
#
# All tapis_job tasks invoke one of the TWO monolithic Tapis apps:
#   - subside-h2i-opera-analysis  (discover, download-opera; STAGE env var)
#   - subside-werc-opera-analysis (4 WERC stages; STAGE env var + path env vars)
# Stage-specific Tapis app registrations are intentionally NOT used — the
# pipeline supplies per-task STAGE / paths / resources via task overrides.

id: subside-werc-opera
type: workflow
description: |
  Full SUBSIDE WERC pipeline: discovers OPERA DISP-S1 products, downloads +
  crops, builds the displacement stack, picks a reference (auto or manual),
  estimates per-pixel velocity, and exports cumulative + velocity GeoTIFFs.
owner: ${apiUserId}
group_id: subside-ops
tenant_id: portals

params:
  start_date:          {type: string, required: true}
  end_date:            {type: string, required: true}
  aoi_geojson_uri:     {type: string, required: true}
  earthdata_netrc_uri: {type: string, required: false}
  reference_mode:      {type: string, default: "auto", enum: [auto, manual, none]}
  reference_lat:       {type: number, required: false}
  reference_lon:       {type: number, required: false}
  anchor_radius_m:     {type: integer, default: 5000}
  n_reference_pixels:  {type: integer, default: 25}
  num_workers:         {type: integer, default: 4}
  min_overlap_percent: {type: number, default: 50.0}
  allocation:          {type: string, required: true}
  update_conda_env:    {type: string, default: "false"}

tasks:

  - id: discover
    type: tapis_job
    depends_on: []
    description: Fast frame + product discovery; fails the pipeline early if no products.
    tapis_job_def:
      appId: subside-h2i-opera-analysis
      appVersion: "0.1.0"
      name: subside-discover-{{ args.start_date }}-{{ args.end_date }}
      nodeCount: 1
      coresPerNode: 2
      memoryMB: 8000
      maxMinutes: 30
      fileInputs:
        - name: run-config
          sourceUrl: "{{ workflow.inline_files.run_config_uri }}"
        - name: aoi-geojson
          sourceUrl: "{{ args.aoi_geojson_uri }}"
        - name: earthdata-netrc
          sourceUrl: "{{ args.earthdata_netrc_uri }}"
      parameterSet:
        envVariables:
          - {key: STAGE, value: "preflight"}
          - {key: UPDATE_CONDA_ENV, value: "{{ args.update_conda_env }}"}
        schedulerOptions:
          - name: "TACC Allocation"
            arg: "{{ args.allocation }}"

  - id: download-opera
    type: tapis_job
    depends_on:
      - id: discover
    description: Heavy NetCDF download + crop + preview + archive.
    tapis_job_def:
      appId: subside-h2i-opera-analysis
      appVersion: "0.1.0"
      name: subside-download-opera-{{ args.start_date }}-{{ args.end_date }}
      nodeCount: 1
      coresPerNode: 16
      memoryMB: 128000
      maxMinutes: 240
      fileInputs:
        - name: run-config
          sourceUrl: "{{ workflow.inline_files.run_config_uri }}"
        - name: aoi-geojson
          sourceUrl: "{{ args.aoi_geojson_uri }}"
        - name: earthdata-netrc
          sourceUrl: "{{ args.earthdata_netrc_uri }}"
      parameterSet:
        envVariables:
          - {key: STAGE, value: "run"}
          - {key: UPDATE_CONDA_ENV, value: "{{ args.update_conda_env }}"}
        schedulerOptions:
          - name: "TACC Allocation"
            arg: "{{ args.allocation }}"

  - id: build-stack
    type: tapis_job
    depends_on:
      - id: download-opera
    description: Combine per-product NetCDFs into one displacement stack NetCDF.
    tapis_job_def:
      appId: subside-werc-opera-analysis
      appVersion: "0.1.0"
      name: subside-build-stack-{{ args.start_date }}-{{ args.end_date }}
      nodeCount: 1
      coresPerNode: 8
      memoryMB: 32000
      maxMinutes: 60
      fileInputs:
        - name: netcdf-source
          sourceUrl: "{{ tasks.download-opera.outputs.archive }}/output/OPERA_L3_DISP-S1"
          targetPath: "inputs/OPERA_L3_DISP-S1"
      parameterSet:
        envVariables:
          - {key: STAGE,            value: "build-stack"}
          - {key: NETCDF_DIR,       value: "inputs/OPERA_L3_DISP-S1"}
          - {key: OUTPUT_STACK,     value: "output/displacement_stack.nc"}
          - {key: OUTPUT_PRODUCTS,  value: "output/disp_products.json"}
          - {key: UPDATE_CONDA_ENV, value: "{{ args.update_conda_env }}"}
        schedulerOptions:
          - name: "TACC Allocation"
            arg: "{{ args.allocation }}"

  - id: compute-reference
    type: tapis_job
    depends_on:
      - id: build-stack
    description: Pick reference pixels (auto or manual); subtract offset from stack.
    tapis_job_def:
      appId: subside-werc-opera-analysis
      appVersion: "0.1.0"
      name: subside-compute-reference-{{ args.start_date }}-{{ args.end_date }}
      nodeCount: 1
      coresPerNode: 8
      memoryMB: 32000
      maxMinutes: 60
      fileInputs:
        - name: stack-input
          sourceUrl: "{{ tasks.build-stack.outputs.archive }}/output/displacement_stack.nc"
          targetPath: "inputs/displacement_stack.nc"
      parameterSet:
        envVariables:
          - {key: STAGE,              value: "compute-reference"}
          - {key: STACK_INPUT,        value: "inputs/displacement_stack.nc"}
          - {key: OUTPUT_STACK,       value: "output/displacement_stack_referenced.nc"}
          - {key: OUTPUT_SUMMARY,     value: "output/reference_summary.json"}
          - {key: REFERENCE_MODE,     value: "{{ args.reference_mode }}"}
          - {key: ANCHOR_DIR,         value: "output/anchors"}
          - {key: ANCHOR_RADIUS_M,    value: "{{ args.anchor_radius_m }}"}
          - {key: N_REFERENCE_PIXELS, value: "{{ args.n_reference_pixels }}"}
          - {key: REFERENCE_LAT,      value: "{{ args.reference_lat }}"}
          - {key: REFERENCE_LON,      value: "{{ args.reference_lon }}"}
          - {key: UPDATE_CONDA_ENV,   value: "{{ args.update_conda_env }}"}
        schedulerOptions:
          - name: "TACC Allocation"
            arg: "{{ args.allocation }}"

  - id: estimate-velocity
    type: tapis_job
    depends_on:
      - id: compute-reference
    description: Per-pixel linear-fit velocity from the referenced stack.
    tapis_job_def:
      appId: subside-werc-opera-analysis
      appVersion: "0.1.0"
      name: subside-estimate-velocity-{{ args.start_date }}-{{ args.end_date }}
      nodeCount: 1
      coresPerNode: 8
      memoryMB: 32000
      maxMinutes: 60
      fileInputs:
        - name: stack-input
          sourceUrl: "{{ tasks.compute-reference.outputs.archive }}/output/displacement_stack_referenced.nc"
          targetPath: "inputs/displacement_stack_referenced.nc"
      parameterSet:
        envVariables:
          - {key: STAGE,            value: "estimate-velocity"}
          - {key: STACK_INPUT,      value: "inputs/displacement_stack_referenced.nc"}
          - {key: OUTPUT_VELOCITY,  value: "output/velocity.nc"}
          - {key: OUTPUT_SUMMARY,   value: "output/velocity_summary.json"}
          - {key: UPDATE_CONDA_ENV, value: "{{ args.update_conda_env }}"}
        schedulerOptions:
          - name: "TACC Allocation"
            arg: "{{ args.allocation }}"

  - id: export-geotiffs
    type: tapis_job
    depends_on:
      - id: estimate-velocity
    description: Reproject + write cumulative displacement and velocity GeoTIFFs.
    tapis_job_def:
      appId: subside-werc-opera-analysis
      appVersion: "0.1.0"
      name: subside-export-geotiffs-{{ args.start_date }}-{{ args.end_date }}
      nodeCount: 1
      coresPerNode: 4
      memoryMB: 16000
      maxMinutes: 30
      fileInputs:
        - name: stack-input
          sourceUrl: "{{ tasks.compute-reference.outputs.archive }}/output/displacement_stack_referenced.nc"
          targetPath: "inputs/displacement_stack_referenced.nc"
        - name: velocity-input
          sourceUrl: "{{ tasks.estimate-velocity.outputs.archive }}/output/velocity.nc"
          targetPath: "inputs/velocity.nc"
        - name: reference-summary
          sourceUrl: "{{ tasks.compute-reference.outputs.archive }}/output/reference_summary.json"
          targetPath: "inputs/reference_summary.json"
      parameterSet:
        envVariables:
          - {key: STAGE,             value: "export-geotiffs"}
          - {key: STACK_INPUT,       value: "inputs/displacement_stack_referenced.nc"}
          - {key: VELOCITY_INPUT,    value: "inputs/velocity.nc"}
          - {key: OUTPUT_DIR_PATH,   value: "output"}
          - {key: REFERENCE_SUMMARY, value: "inputs/reference_summary.json"}
          - {key: UPDATE_CONDA_ENV,  value: "{{ args.update_conda_env }}"}
        schedulerOptions:
          - name: "TACC Allocation"
            arg: "{{ args.allocation }}"

  - id: publish
    type: function
    runtime: "python:3.11"
    installer: pip          # required by the Tapis Workflows FunctionTask schema
    packages: []            # stdlib-only; no third-party deps to install
    depends_on:
      - id: export-geotiffs
    description: |
      Stdlib-only Python. Reads every summary JSON from the per-stage archives
      and emits a unified subside-run-manifest.json for the UI.
    inputs:
      preflight_manifest_uri:    "{{ tasks.discover.outputs.archive }}/output/preflight-manifest.json"
      h2i_run_manifest_uri:      "{{ tasks.download-opera.outputs.archive }}/output/run-manifest.json"
      reference_summary_uri:     "{{ tasks.compute-reference.outputs.archive }}/output/reference_summary.json"
      velocity_summary_uri:      "{{ tasks.estimate-velocity.outputs.archive }}/output/velocity_summary.json"
      export_summary_uri:        "{{ tasks.export-geotiffs.outputs.archive }}/output/export_summary.json"
    code: |
      import json, os, urllib.request

      def _load(env_key):
          uri = os.environ[env_key]
          if uri.startswith("tapis://"):
              raise RuntimeError("Configure tapis-files fetch in your workflows runner.")
          with urllib.request.urlopen(uri) as f:
              return json.load(f)

      manifest = {
          "pipeline": "subside-werc-opera",
          "schema_version": "v0",
          "preflight": _load("INPUTS_PREFLIGHT_MANIFEST_URI"),
          "h2i": _load("INPUTS_H2I_RUN_MANIFEST_URI"),
          "reference": _load("INPUTS_REFERENCE_SUMMARY_URI"),
          "velocity": _load("INPUTS_VELOCITY_SUMMARY_URI"),
          "export": _load("INPUTS_EXPORT_SUMMARY_URI"),
      }
      with open(os.environ["OUTPUT_DIR"] + "/subside-run-manifest.json", "w") as f:
          json.dump(manifest, f, indent=2, sort_keys=True, default=str)
